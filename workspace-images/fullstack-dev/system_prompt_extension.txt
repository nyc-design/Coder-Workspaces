-- Fullstack / FastAPI + Vite/React --
Use project-standard uv / python -m commands for the backend. Define Pydantic request/response contracts as stubs before implementing endpoints; these drive the frontend OpenAPI types described above. Validate each endpoint's inputs and responses before moving on.
